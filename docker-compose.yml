services:

  eureka-server:
    build:
      context: ./eureka-server
    container_name: lms-eureka-server
    restart: unless-stopped
    environment:
      PORT: 8761
    ports:
      - "8761:8761"
    networks:
      - lms-network

  user-service:
    build:
      context: ./user-service
    container_name: lms-user-service
    restart: unless-stopped
    depends_on:
      - eureka-server
    environment:
      PORT: 8081
      MONGODB_URI: ${MONGODB_URI}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION_MS: ${JWT_EXPIRATION_MS:-7200000}
      EUREKA_SERVER_URL: http://eureka-server:8761/eureka/
    ports:
      - "8081:8081"
    networks:
      - lms-network

  course-service:
    build:
      context: ./course-service
    container_name: lms-course-service
    restart: unless-stopped
    depends_on:
      - eureka-server
    environment:
      PORT: 8082
      MONGODB_COURSE_URI: ${MONGODB_COURSE_URI}
      JWT_SECRET: ${JWT_SECRET}
      EUREKA_SERVER_URL: http://eureka-server:8761/eureka/
      CLOUDINARY_CLOUD_NAME: ${CLOUDINARY_CLOUD_NAME}
      CLOUDINARY_API_KEY: ${CLOUDINARY_API_KEY}
      CLOUDINARY_API_SECRET: ${CLOUDINARY_API_SECRET}
      PAYPAL_MODE: ${PAYPAL_MODE}
      PAYPAL_CLIENT_ID: ${PAYPAL_CLIENT_ID}
      PAYPAL_CLIENT_SECRET: ${PAYPAL_CLIENT_SECRET}
    ports:
      - "8082:8082"
    networks:
      - lms-network

  quiz-service:
    build:
      context: ./quiz-service
    container_name: lms-quiz-service
    restart: unless-stopped
    depends_on:
      - eureka-server
    environment:
      PORT: 8083
      MONGODB_QUIZ_URI: ${MONGODB_QUIZ_URI}
      JWT_SECRET: ${JWT_SECRET}
      EUREKA_SERVER_URL: http://eureka-server:8761/eureka/
    ports:
      - "8083:8083"
    networks:
      - lms-network

  api-gateway:
    build:
      context: ./api-gateway
    container_name: lms-api-gateway
    restart: unless-stopped
    depends_on:
      - eureka-server
    environment:
      PORT: 8080
      EUREKA_SERVER_URL: http://eureka-server:8761/eureka/
    ports:
      - "8080:8080"
    networks:
      - lms-network

networks:
  lms-network:
    driver: bridge